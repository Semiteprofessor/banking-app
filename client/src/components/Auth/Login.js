/** @format */

import React, { useState } from "react";
import { StyleSheet } from "react-native";
import { Input, Form, Item, Button, View, Text } from "native-base";

function Login({ navigation }) {
  return (
    <View>
      <Text>Welcome to Login Page</Text>
    </View>
  );
}

export default Login;
